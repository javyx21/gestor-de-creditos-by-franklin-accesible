__pycache__/
*.pyc
.venv/
venv/
data/
*.egg-info/
.pytest_cache/
desktop.ini
Thumbs.db

# Artefactos de empaquetado con PyInstaller (ver README.md, sección "Empaquetado portable")
build/
dist/
*.spec

# Datos reales de clientes (nombres, cédulas) — nunca deben ir al repositorio.
# Regla general: NINGÚN archivo Excel (de cualquier nombre, en cualquier carpeta)
# debe subirse jamás, ya que suelen contener datos reales de clientes.
*.xlsx
*.xls
*.xlsm

# Entradas explícitas anteriores (redundantes con las reglas de arriba, se
# dejan por historial/claridad — no hace falta agregar nuevas aquí).
MachoteBaseDeDatos.xlsx
recursos/calculadora.xlsx
recursos/reporte.xlsx
base de clientes activos .xlsx

# Capturas de pantalla con datos reales de clientes (nombres/cédulas visibles),
# dejadas por el usuario para diagnóstico manual — nunca deben ir al repositorio.
captura orden de cancelados .jpg
captura fallo en cancelados .jpg
